/* Функция getTestTakers принимает в качестве параметров
 * список имён студентов и количество вариантов на грядущем тесте N.
 * Она должна назначить каждому студенту номер варианта от 1 до N включительно
 * и вернуть назначение в виде списка объектов класса TestTakers.
 * 
 * TestTakers в ответе должны быть перечислены в том же порядке,
 * что и соответствующие студенты в заданном списке имён.
 * 
 * Назначенные варианты должны начинаться с 1, увеличиваться на 1
 * для каждого следующего студента, а после достижения значения N
 * возвращаться обратно к значению 1, и так далее.
 * 
 * Например, для списка ["Alice", "Bob", "Carl", "David"] и N = 2
 * у Alice и Carl будет 1 вариант, а у Bob и David -- 2.
 * 
 * Постарайтесь реализовать этот функционал функции getTestTakers
 * одним выражением.
 */

class TestTaker {
  final String name;
  final int variant;
  TestTaker(this.name, this.variant);
}

List<TestTaker> getTestTakers(List<String> names, int N) => /* ваш код тут */;
